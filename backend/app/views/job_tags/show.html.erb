<p id="notice"><%= notice %></p>

<p>
  <strong>Job:</strong>
  <%= @job_tag.job_id %>
</p>

<p>
  <strong>Tag:</strong>
  <%= @job_tag.tag_id %>
</p>

<%= link_to 'Edit', edit_job_tag_path(@job_tag) %> |
<%= link_to 'Back', job_tags_path %>
